# Qalculate! Qt UI - UNI PROJECT

## Requirements
* Qt5 (≥ 5.6) or Qt6
* libqalculate (Most Recent State on Github)
* bash
* TeX-Live aka pdflatex & pdtoppm

In a terminal window in the top source code directory run
* `qmake`
* `make`
* `make install` *(as root, e.g. `sudo make install`)*